const navItems = document.querySelector('.gnb>li');
navItems.forEach(Item => item.addEvetListener
  ('mouseover',() => const sub = item.addEvetListener(
    const sub = item querySelector('.sub');

    sub.style.maxHeight = sub.scrollHeight + 'px'
  )
  
  
  )